import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class ResepMasakanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Resep Masakan Nusantara'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Ini adalah halaman resep masakan'),
      ),
    );
  }
}