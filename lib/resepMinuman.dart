import 'package:flutter/material.dart';

import 'package:flutter/material.dart';

class ResepMinumanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Resep Minuman Nusantara'),
        centerTitle: true,
      ),
      body: Center(
        child: Text('Ini adalah halaman resep minuman'),
      ),
    );
  }
}